# Directory for storing user uploads (profile photos, etc.)
export UPLOADS_DIRECTORY="/path/to/uploads"

# Directory for storing test uploads (used during test runs)
export UPLOADS_DIRECTORY_TEST="/path/to/test/uploads"
